(() => {
  const zh = {
    skip:'跳转至正文',menu:'菜单',navServices:'申请辅导',navApproach:'我们的方式',navStudio:'关于叙途',navContact:'联系我们',
    heroEyebrow:'留学申请与文书辅导',heroTitle:'你的故事，<br>值得一个',heroEm:'新篇章。',heroDescription:'从经历梳理到清晰表达，叙途与你一起找到申请的方向。让你的经历、思考与愿望，在文字中真实呈现。',heroCta:'找到你的方向',heroFootnote:'美国本科 · 硕士 · 博士申请',photoLabel:'把属于你的故事，讲清楚。',photoCaption:'新的视角，新的可能。',
    introEyebrow:'从了解你开始',introTitle:'每一份申请背后，<br>都有一个值得被了解的人。',introDescription:'你关心的问题、投入过的研究，以及改变方向的经历，都值得被认真看见。我们帮助你把这些片段串联起来，形成清晰、连贯、忠于自己的申请表达。',
    servicesEyebrow:'申请辅导',servicesTitle:'不同的方向，<br>同样用心的陪伴。',servicesDescription:'从你的起点出发，<br>走向你期待的下一站。',scope:'我们可以一起完成',
    undergradTag:'开启第一章',undergradTitle:'本科申请',undergradDescription:'梳理兴趣、经历与成长，让招生官看见成绩之外的你。',undergrad1:'申请方向与规划',undergrad2:'个人主文书构思',undergrad3:'补充文书与修改',undergrad4:'活动与经历描述',
    mastersTag:'探索新方向',mastersTitle:'硕士申请',mastersDescription:'连接已有经历与未来目标，用清晰的逻辑说明你的选择与准备。',masters1:'项目匹配与申请策略',masters2:'目的陈述（SOP）',masters3:'个人陈述与文书修改',masters4:'简历梳理与反馈',
    phdTag:'追问更深处',phdTitle:'博士与科研申请',phdDescription:'梳理科研经历、学术兴趣与未来问题，让你的研究方向与思考更清晰。',phd1:'研究方向与项目匹配',phd2:'目的陈述（SOP）',phd3:'研究陈述与学术简历',phd4:'结构、逻辑与学术表达',servicesNote:'从一篇文书的打磨，到整套申请材料的连贯表达。',
    approachEyebrow:'叙途的方式',approachTitle:'好的表达，<br>始于认真的',approachEm:'倾听。',approachDescription:'在讨论文字之前，我们先了解写下这些文字的你。',process1Title:'理解你的故事',process1Description:'回顾经历、兴趣与目标，找到那些能够说明你是谁、在意什么的具体时刻。',process2Title:'找到贯穿的线索',process2Description:'明确申请方向与文书结构，把过去的积累与下一阶段的学术目标连接起来。',process3Title:'带着目的去打磨',process3Description:'通过具体反馈，逐步调整结构、细节和语言，让表达更清晰，也保留你的声音。',
    meaning1:'讲述自己的故事',meaning2:'走向自己的未来',studioEyebrow:'叙途，名字里的初心',studioTitle:'把你的故事，<br>与未来的路连接起来。',studioDescription:'叙途是一间专注申请辅导与文书表达的教育咨询工作室。我们相信，申请材料应当从你真实的经历与愿望中生长出来。',studioDescription2:'我们带来认真的提问、坦诚的反馈和新的视角。你带来属于自己的故事。',mentorTitle:'学术深度，文字视野。',mentorAdmissions:'导师团队的申请经历涵盖哥伦比亚大学、宾夕法尼亚大学和康奈尔大学的本科录取，以及哈佛大学、麻省理工学院和普林斯顿大学的理工科博士录取。',mentorWriting:'团队亦有以最高荣誉（summa cum laude）毕业于哥伦比亚大学哥伦比亚学院英语专业的成员，为个人叙事带来文学视角。',value1:'真实的声音',value2:'用心的策略',value3:'清晰的表达',
    questionsEyebrow:'在开始之前',questionsTitle:'让每一步，<br>更清晰。',faq1Question:'可以只修改一篇文书吗？',faq1Answer:'可以。辅导可以聚焦于单篇文书，也可以关注整套材料之间的配合。我们从你具体的表达需求出发。',faq2Question:'开始之前需要有完整初稿吗？',faq2Answer:'不需要。你可以带着想法、笔记或已有草稿开始。我们可以协助你梳理经历、形成提纲，或修改已经开始的写作。',faq3Question:'文书辅导具体包括什么？',faq3Answer:'通过提问、结构反馈和语言建议，帮助你清楚地表达自己的想法。你始终是文书的作者，所有内容都应基于真实经历。录取决定由各院校独立作出。',
    closingEyebrow:'属于你的下一章',closingTitle:'从真实的<em>你</em>，开始。',contactIntro:'告诉我们你目前的申请阶段，以及希望获得的帮助。',inquiryTitle:'给我们留言',inquiryName:'你的姓名',inquiryEmail:'你的邮箱',inquiryMessage:'留言内容',inquiryHint:'欢迎介绍申请阶段、时间安排，以及你需要的帮助。10–5,000 字符。',inquiryPrivacy:'我们使用你的姓名、邮箱和留言回复咨询。消息通过邮件服务发送。',inquirySend:'发送留言',contactAlternative:'你也可以在小红书搜索 XUTU STUDIO，私信联系我们。',contactPlatform:'小红书 · XIAOHONGSHU',contactRecognition:'认准写有「大藤文书工作室」的绿色书本与藤叶头像。',copyAccount:'复制账号名称',footerTagline:'叙自己的故事，走自己的路。',backTop:'回到顶部',footerDescriptor:'独立教育咨询工作室'
  };
  const nodes = [...document.querySelectorAll('[data-i18n]')];
  const en = Object.fromEntries(nodes.map(node => [node.dataset.i18n,node.innerHTML]));
  const languageButtons = document.querySelectorAll('[data-lang]');
  function setLanguage(lang) {
    const isZh = lang === 'zh';
    document.querySelector('#copy-status').textContent = ''; 
    document.documentElement.lang = isZh ? 'zh-CN' : 'en';
    const content = isZh ? zh : en;
    nodes.forEach(node => { node.innerHTML = content[node.dataset.i18n] ?? en[node.dataset.i18n]; });
    languageButtons.forEach(button => button.setAttribute('aria-pressed',String(button.dataset.lang === lang)));
    document.title = isZh ? 'XuTu 叙途 — 你的故事，新的篇章。' : 'XuTu 叙途 — Your story. Your next chapter.';
    updateInquiryText();
    document.querySelector('.hero-visual img').alt = isZh ? '阳光穿过石拱门，照亮通往绿意盎然的校园庭院的小径' : 'A sunlit path through a stone arch into a leafy academic courtyard';
  }
  languageButtons.forEach(button => button.addEventListener('click',() => setLanguage(button.dataset.lang)));
  const menuButton = document.querySelector('.menu-toggle');
  const mobileNav = document.querySelector('#mobile-nav');
  function closeMenu(){ menuButton.setAttribute('aria-expanded','false'); mobileNav.hidden = true; }
  menuButton.addEventListener('click',() => { const open = menuButton.getAttribute('aria-expanded') === 'true'; menuButton.setAttribute('aria-expanded',String(!open)); mobileNav.hidden = open; });
  mobileNav.querySelectorAll('a').forEach(link => link.addEventListener('click',closeMenu));
  document.addEventListener('keydown',event => { if(event.key === 'Escape' && !mobileNav.hidden){ closeMenu(); menuButton.focus(); } });
  matchMedia('(min-width: 801px)').addEventListener('change',event => { if(event.matches) closeMenu(); });
  const copyButton = document.querySelector('#copy-account');
  copyButton.addEventListener('click', async () => {
    copyButton.disabled = true;
    try {
      await navigator.clipboard.writeText('XUTU STUDIO');
      document.querySelector('#copy-status').textContent = document.documentElement.lang === 'zh-CN'
        ? '已复制！打开小红书，搜索 XUTU STUDIO，私信联系我们。'
        : 'Copied! Open Xiaohongshu, search XUTU STUDIO, and send us a message.';
    } catch {
      const range = document.createRange();
      range.selectNodeContents(document.querySelector('#xiaohongshu-account'));
      const selection = window.getSelection();
      if (selection) { selection.removeAllRanges(); selection.addRange(range); }
      document.querySelector('#copy-status').textContent = document.documentElement.lang === 'zh-CN'
        ? '请复制上方账号名称 XUTU STUDIO，然后在小红书中搜索。'
        : 'Copy the account name XUTU STUDIO above, then search for it in Xiaohongshu.';
    } finally {
      copyButton.disabled = false;
    }
  });
  const inquiryForm = document.querySelector('#inquiry-form');
  const inquiryFields = document.querySelector('#inquiry-fields');
  const inquiryButton = document.querySelector('#inquiry-submit');
  const inquiryStatus = document.querySelector('#inquiry-status');
  let inquiryState = 'loading';
  let submissionId = null;
  let lastPayload = null;
  const inquiryMessages = {
    en: {loading:'Checking message availability…', ready:'', unavailable:'The message form is temporarily unavailable. Please contact us on Xiaohongshu below.', sending:'Sending your message…', success:'Your message has been sent. We’ll reply to the email address you provided.', error:'We couldn’t confirm your message was sent. Your text is still here—please try again or contact us on Xiaohongshu.', rate:'Please wait a few minutes before trying again, or reach us on Xiaohongshu.', validation:'Please check your name, email, and message (10–5,000 characters).', button:'Send message', busy:'Sending…'},
    zh: {loading:'正在检查留言服务…', ready:'', unavailable:'留言表单暂时无法使用，请通过下方小红书账号联系我们。', sending:'正在发送留言…', success:'留言已发送。我们会通过你填写的邮箱回复。', error:'暂时无法确认留言是否发送成功。内容已保留，请重试或通过小红书联系我们。', rate:'请稍等几分钟后重试，或通过小红书联系我们。', validation:'请检查姓名、邮箱和留言内容（10–5,000 字符）。', button:'发送留言', busy:'发送中…'}
  };
  function updateInquiryText() {
    const language = document.documentElement.lang === 'zh-CN' ? 'zh' : 'en';
    inquiryStatus.textContent = inquiryMessages[language][inquiryState];
    inquiryButton.textContent = inquiryMessages[language][inquiryState === 'sending' ? 'busy' : 'button'];
    inquiryForm.dataset.state = inquiryState;
  }
  function setInquiryState(state) { inquiryState=state; updateInquiryText(); }
  async function checkInquiry() {
    try {
      const response=await fetch('/api/contact', {headers:{Accept:'application/json'},signal:AbortSignal.timeout(10000)});
      const config=await response.json();
      if(!response.ok || config.available!==true) throw new Error('unavailable');
      inquiryFields.disabled=false;
      setInquiryState('ready');
    } catch {inquiryFields.disabled=true;setInquiryState('unavailable');}
  }
  inquiryForm.addEventListener('submit',async event => {
    event.preventDefault();
    if(inquiryFields.disabled || !inquiryForm.reportValidity())return;
    const values=new FormData(inquiryForm);
    const payload={name:String(values.get('name')||'').trim(),email:String(values.get('email')||'').trim(),message:String(values.get('message')||'').trim(),website:String(values.get('website')||'')};
    if(!payload.name || payload.message.length<10 || payload.website){setInquiryState('validation');return;}
    const signature=JSON.stringify(payload);
    if(signature!==lastPayload){submissionId=crypto.randomUUID();lastPayload=signature;}
    inquiryFields.disabled=true;setInquiryState('sending');
    try {
      const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json',Accept:'application/json'},body:JSON.stringify({...payload,submissionId}),signal:AbortSignal.timeout(20000)});
      const result=await response.json();
      if(!response.ok || result.ok!==true){
        setInquiryState(result.code==='unavailable'?'unavailable':result.code==='rate'?'rate':result.code==='validation'?'validation':'error');
      }else{
        inquiryForm.reset();submissionId=null;lastPayload=null;setInquiryState('success');
      }
    }catch{setInquiryState('error');}
    finally{inquiryFields.disabled=inquiryState==='unavailable';}
  });
  checkInquiry();
  document.querySelector('#year').textContent = new Date().getFullYear();
})();
